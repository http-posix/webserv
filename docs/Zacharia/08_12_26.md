## 12th of August

I want to write a ton of tests for the config parser. 
    First I think I must check whether the parser works correctly in case of correct config files.
    Second comes the part where I must correctly catch incorrect config files so we don't crash later on.
        This leads me to wonder how the testing works in case of errors.
    
Correct Config Files
---
Let's looks at what we must be able to handle again:
    - Define all the interface:port pairs on which your server will listen to.
        I believe this means we can have multiple hostnames as well. (might be wrong though)
    - Set up default error pages.
        This means; an error code plus an html file. -> Should I check whether the html file exists in the parser
        or is this something we catch later on, output to the logger and then have a failsafe?
    - Set the maximium allowed size for client request bodies. 
        For later, but I should check whether non-integer numbers are allowed.

-   Do I need root for the server by itself?
        It seems not, but I do currently have it implemented.

So things we can check:
1. See whether we should have multiple names/hostnames/ip adresses.
2. Check multiple ports (is there a maximum?)
3. Check error pages -> number then URI.
4. Check multiple error pages and correct usage.
5. Overwrite error pages
6. Check if root error page is allowed (not a file though)
7. Check maximum allowed size.
    
Then we have location Rules:
    - Accepted methods.
    - Redirection with URI.
        Does this need to redirection number as well? I believe not which means I have to fix that.
    - Default file to server when the requested resource is a directory.
        This leaves some ambiguity.
    - Enabling or disabling directory listing. 
        This is just an easy yes/no check. I can initialize this to `no` and only have it enabled if specified.
    - Directory where the requested file should be located. (if /zach is rooted to /tmp/hello, /zach/goodbye will search for /tmp/hello/goodbye)
        I believe this is what root can do in this scenario.
    - Authorization for uploading.
        Is this not part of the allowed methods?
    - Storage location for uploading files.
        Need to check whether this is a directory I presume?
