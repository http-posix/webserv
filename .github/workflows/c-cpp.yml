name: C/C++ CI

on:
  pull_request:
    branches: [ " main " ]
    types: [opened, reopened, synchronize]
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: make tests
      run: make tests
